import React from "react";
import ReactDOM from "react-dom/client";
import AbesCollegePage from "./assignment";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <AbesCollegePage />
  </React.StrictMode>
);
